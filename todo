view
view-panel
button
tab
view-tab
tab-button
button stack
increase width of panels

fix properties: translucent, outside-line, --scola-dark-shadow-line etc.
add default slot element etc to nodeelement

set colors of scrollbar etc correctly
line shadow for translucent fill
remove template from view
icon loading (spin)
add themes
move helpers

form-fill/submit
// lock form when sending
// test clear all inputs
// test setdata with all inputs
// value getter
rename setValueText in inputs

popout
dialog
// position of popout (top right left bottom of screen)
// test with nested menu
// nested popouts (agree + agree in iOS)

alert
app
auth
input-popout
input-file with dragndrop

// cap queuer stream size
// reverse lookup of strings

// create models
// list
// add/set/del
// pagination + swipe
// search
// small/wide
// select multiple
// button vs list item
// show all
// unfold item with panel (for inline editing)

// authorize
//  return credentials from Authorizer.authorize
//  then password can be sent along request
// open popout by default? or modal...
// pass input/init from fetch to auth to resolve/reject based on scope
//  server must return the scope of the authorization
const auth = {
  'post.create': 1,
  'post.delete': 2
}

// profile
//   store tabs toggle etc.
//   store settings in profile, but save only at will
//   Profile.get/set/del/add + endpoints (list + object)

// ARIA

// event-stream

// fix ts-lit-plugin in tsconfig.json

// view: ondisconnectedcallbacK: remove entry from url
// implement tooltip

// create something like:
// https://material.io/components/menus#exposed-dropdown-menu (font picker)
// https://material.io/design/color/dark-theme.html#ui-application (KPI)
// try to recreate Sonos or Notes

/* @media not all and (max-width: 1024px) {
  slot[name="after"],
  slot[name="after"]::slotted(*) {
    position: relative;
  }

  slot[name="before"] {
    position: relative;
  }
}

@media (min-width: 768px) {
  slot[name="before"]::slotted(*) {
    position: relative;
  }
} */
